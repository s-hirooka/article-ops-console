# --- P1: Windows依存の解消 ---
google-ads>=25.1.0            # KeywordQueryRunner.exe の置き換え（GenerateKeywordHistoricalMetrics）
python-dotenv>=1.0.1          # ローカル開発の .env 読み込み
pillow>=11.0                  # アイキャッチ生成（Yu Gothic → Noto Sans JP）
cryptography>=43.0            # OAuth リフレッシュトークンの暗号化（Fernet）
google-auth>=2.35            # サーバーサイド Google OAuth（トークン交換 / 更新）
requests>=2.32                # OAuth トークンエンドポイントへの HTTP

# --- 以降のフェーズで追加予定 ---
# fastapi>=0.115
# uvicorn[standard]>=0.32
# sqlalchemy>=2.0
# psycopg[binary]>=3.2
# alembic>=1.13
# anthropic>=0.40
# httpx>=0.27
